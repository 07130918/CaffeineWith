<div class="setting">
 <div class="full">
  <% @logs.each do |one| %>
   <div class="one-log">
      <p><%= one.text %></p>
      <div class="destroy-btn">
       <%= link_to "destroy this log", "/caffes/#{:caffe_id}/logs/#{:id}", method: :delete, class: "one-destroy" %>
                                       <!--ここprefixにしたい-->
      </div>
   </div>
  <% end %>
  <div class="pagnation">
   <%= paginate(@logs) %>
  </div>
 </div>
</div>